<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>문의하기</h1>
    <h2><%= heading %></h2>
    <ul>
        <% users.forEach(user => { %>
        <li>
            <dl>
                <dt>Name</dt>
                <dd><%= user.name %></dd> 
                <dt>E-mail</dt>
                <dd><%= user.email %></dd> 
            </dl> 
        </li>
        <% }) %>
    </ul>
</body>
</html>